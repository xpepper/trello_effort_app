<%= form_for(@tracked_card) do |f| %>
  <% if @tracked_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tracked_card.errors.count, "error") %> prohibited this tracked_card from being saved:</h2>

      <ul>
      <% @tracked_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, :class => 'span4' %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, :class => 'span4' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
